# Core Framework
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# Computer Vision & Image Processing
opencv-python==4.8.1.78
Pillow>=10.2.0
numpy>=1.24.0,<2.0.0
scipy==1.11.3

# Face Detection
mediapipe==0.10.21
mtcnn==0.1.1
# retinaface>=1.1.1  # Commented out due to tensorflow conflict

# Deep Learning & FaceNet
# Lưu ý: facenet-pytorch==2.6.0 tương thích tốt với torch 2.2.x / torchvision 0.17.x
# Nếu cần CUDA, cài torch/torchvision bằng index CUDA riêng bên ngoài tệp này
torch>=2.2.0,<2.3.0
torchvision>=0.17.0,<0.18.0
facenet-pytorch==2.6.0

# Machine Learning
scikit-learn==1.3.1
pandas==2.0.3

# Database
SQLAlchemy==2.0.21
Flask-SQLAlchemy==3.0.5

# Data Augmentation
albumentations==1.3.1
imgaug==0.4.0

# Visualization & Metrics
matplotlib==3.7.2
seaborn==0.12.2
plotly==5.16.1

# Utilities
tqdm==4.66.1
python-dotenv==1.0.0
requests==2.31.0
protobuf==4.25.3  # Pin để Mediapipe ổn định, tránh xung đột TF

# Development
pytest==7.4.2
black==23.7.0
flake8==6.0.0

# Optional: For production deployment
gunicorn==21.2.0
redis==4.6.0

# Ghi chú CUDA (không đặt trong requirements vì cần chọn wheel theo GPU):
# - pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
#   hoặc cu118 tùy driver. Sau đó cài facenet-pytorch (không cần --no-deps nếu dùng torch 2.2.x).
